# Production / explicit API host (optional):
# VITE_API_URL=https://api.example.com

# Dev: Vite proxies /api and /socket.io to this URL. Must match backend listen port.
# Default is http://localhost:3000 — only set if your backend uses another port, e.g.:
# VITE_PROXY_TARGET=http://localhost:7001
